appId: com.syntra.app
productName: Syntra

directories:
    output: build
    buildResources: assets

files:
    - "main/**/*"
    - "dist/**/*"
    - "client/**/*"
    - "node_modules/**/*"
    - "package.json"

asar: true

win:
    target:
        - nsis
